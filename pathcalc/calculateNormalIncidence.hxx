//Function Header File for normal incidence calculation
#include <vector>

#ifndef CALCULATENORMALINCIDENCE_H
#define CALCULATENORMALINCIDENCE_H

std::vector<double> calculateNormalIncidence(double center_x, double center_y, double radius, double beam, double x, double y, double incidence, double azimuth);

#endif
